package helsinki.tablecodes.asset;

import ua.com.fielden.platform.entity.fetch.IFetchProvider;
import ua.com.fielden.platform.utils.EntityUtils;
import ua.com.fielden.platform.dao.IEntityDao;

/**
 * Companion object for entity {@link AbstractOwnership}.
 *
 * @author Developers
 *
 */
public interface IAbstractOwnership extends IEntityDao<AbstractOwnership> {

    static final IFetchProvider<AbstractOwnership> FETCH_PROVIDER = EntityUtils.fetch(AbstractOwnership.class).with(
        // TODO: uncomment the following line and specify the properties, which are required for the UI. Then remove the line after.
        // "key", "desc");
        "Please specify the properties, which are required for the UI");

}
